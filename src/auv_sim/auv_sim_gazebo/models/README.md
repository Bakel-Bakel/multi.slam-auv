# models

Drop additional gz-sim model directories here (e.g. detailed seafloor heightmaps,
shipwrecks, or the Project DAVE sensor models) and add the directory to
`GZ_SIM_RESOURCE_PATH` in `launch/sim_gazebo.launch.py`. The default `ocean.sdf`
world embeds its terrain inline so no external models are required to start.
